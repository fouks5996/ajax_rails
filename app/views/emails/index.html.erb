<p id="notice"><%= notice %></p>

<a href="<%=emails_path%>" data-method="POST" data-remote="true" class="btn btn-primary my-3">recevoir un email</a>


<div style="display:flex; flex-direction:row; justify-content:space-around" >
    <div style="height:90vh" >
        <h1>liste d'emails</h1>
        <table>
          <thead>
            <tr>
              <th>Body</th>
              <th>Object</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody id="email-list">
            <% @emails.each do |email| %>
              <tr id="email2-<%= email.id %>">
                <td><%= email.body %></td>
                <td><a href="<%=email_path(email.id)%>" data-method="GET" data-remote="true"   ><%= email.object %></a></td>
                <td><%= link_to 'Edit', edit_email_path(email) %></td>
                <td> <a href="<%= email_path(email)%>" data-method="DELETE" data-remote="true"> TRASH </a> </td>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
      <div style="height:90vh; width:40%; background-color:grey" >
        <h1>Détails email</h1>
        <div id="email_show">
        </div>
    </div>
</div>